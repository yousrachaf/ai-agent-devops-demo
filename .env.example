# ════════════════════════════════════════════════════════════════
# FICHIER .env.example — AI Agent DevOps Demo
# ════════════════════════════════════════════════════════════════
#
# IMPORTANT : Ce fichier est un template. Pour l'utiliser :
#   1. Copier ce fichier : cp .env.example .env
#   2. Remplir les valeurs réelles dans .env
#   3. Ne JAMAIS commit .env dans Git
#
# ════════════════════════════════════════════════════════════════

# ────────────────────────────────────────────────────────────────
# CLAUDE API (Anthropic) — REQUIS
# ────────────────────────────────────────────────────────────────
# Obtenir sur : https://console.anthropic.com/settings/keys
# Format : sk-ant-api03-...
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Modèle à utiliser
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# Timeout pour les appels Claude en millisecondes (défaut: 30 secondes)
ANTHROPIC_TIMEOUT_MS=30000

# ────────────────────────────────────────────────────────────────
# LANGFUSE — REQUIS (monitoring & observabilité)
# ────────────────────────────────────────────────────────────────
# Obtenir sur : https://cloud.langfuse.com/project/[votre-projet]/settings

# Clé publique (commence par pk-lf-...)
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key-here

# Clé secrète (commence par sk-lf-...)
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key-here

# Host LangFuse (cloud par défaut, ou self-hosted)
LANGFUSE_HOST=https://cloud.langfuse.com

# Activer/désactiver LangFuse (false = pas de traces, utile en test)
LANGFUSE_ENABLED=true

# ────────────────────────────────────────────────────────────────
# SERVEUR HTTP
# ────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# ────────────────────────────────────────────────────────────────
# SÉCURITÉ
# ────────────────────────────────────────────────────────────────
# Activer l'auth par API Key (false = endpoint public)
API_KEY_REQUIRED=false

# Générer : openssl rand -hex 32
API_KEY=your-secret-api-key-change-me-in-production

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=20

# CORS : origines autorisées (séparées par des virgules)
CORS_ORIGINS=http://localhost:3000

# ────────────────────────────────────────────────────────────────
# TESTS
# ────────────────────────────────────────────────────────────────
# Activer les tests de prompts (appellent vraiment Claude API — coûteux)
ENABLE_PROMPT_TESTS=false

# Mock Claude dans les tests unitaires (true = aucun appel réel)
MOCK_CLAUDE_IN_TESTS=true
